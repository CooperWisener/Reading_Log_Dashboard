# ─── Dependencies ─────────────────────────────────────────────────────────────
node_modules/

# ─── Build output ─────────────────────────────────────────────────────────────
# Vite renderer build
dist/
# electron-builder installers + win-unpacked
release/
# legacy electron-builder output name
dist-electron/

# ─── Planning / internal docs (keep out of the public repo) ───────────────────
prompting plan.md
Reading Log Dashboard.md
v2_planning.md

# ─── Local tooling / agent data ───────────────────────────────────────────────
.claude/
CLAUDE.md

# ─── Environment & secrets ────────────────────────────────────────────────────
.env
.env.*
!.env.example

# ─── Logs ─────────────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ─── Editor / OS cruft ────────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# NOTE: example_csv/ is intentionally NOT ignored — it ships as the sample data.
# build/ (placeholder icons) is kept so `npm run package` works on a clean clone.
